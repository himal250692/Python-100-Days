## Day 7 - Branch and Loop Practice

### Typical beginner exercises
1. **Leap year check**
2. **Prime number check**
3. **Multiplication table**
4. **Greatest common divisor / least common multiple**
5. **Armstrong (narcissistic) numbers**

### Example: prime check
```python
num = int(input('Enter a positive integer: '))
is_prime = num > 1
for i in range(2, int(num ** 0.5) + 1):
    if num % i == 0:
        is_prime = False
        break

if is_prime:
    print(f'{num} is prime')
else:
    print(f'{num} is not prime')
```

### Goal
Use conditions and loops together to build problem-solving skills.
